# Never report test runs to the trace service. (Loaded alongside the main application.yml, which
# keeps its other settings; the page-view integration test turns reporting back on for itself and
# points it at a loopback stub.)
usage-reporting.enabled=false
