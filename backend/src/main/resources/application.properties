server.port=8080

# Base URL of the standalone UserAuth service used for authentication.
# In docker-compose this is overridden to http://userauth:9998.
userauth.url=${USERAUTH_URL:http://localhost:9998}
