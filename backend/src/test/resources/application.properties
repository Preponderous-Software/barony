# Tests use an isolated in-memory H2 database (no ./data files, fresh schema per run).
spring.datasource.url=jdbc:h2:mem:baronytest;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
